export default interface TNotification {
    id : string | null,
    description : string | null,
    viewed : Number,
    id_user : string | null,
    link : string | null,
    ref : string | null,
    created_at : string | null,
    id_record : string | null,
}